syntax = "proto3";

import "../order/order.proto";

service UserService {
   rpc find_user(Identifier) returns (User) {}
}


message Identifier {
  string id = 1;
}

message User {
  string id = 1;
  string username = 2;
  repeated Order orders = 3;
}
